
export const goToHomePage = (navigate) => {
    navigate('/')
}

export const goToProfilePage = (navigate, nome) => {
    navigate(`/profile/${nome}`)
}


export const goToProfileProduto = (navigate, nome) => {
    navigate(`/profile/${nome}`)
}